__pycache__/
*.pyc
venv/
.env
*.tmp

# Sensitive / copyrighted materials (tutor's PPTs, generated worksheets)
resources/
worksheets/

# Personal data
calendar.json
progress.json

# Runtime data dir (volume-mounted in production) — Flask app only
/data/

# hsk1-toolkit static data is committed (it's app code, not runtime data)

generator_page.html
explore_site.py
.claude/settings.local.json
